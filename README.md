#Simple Restful Service in Spring Boot

A simple application uses h2 DB and with on controller. Added container facility, the application is dockerized.
You can use docker build and docker run commands

